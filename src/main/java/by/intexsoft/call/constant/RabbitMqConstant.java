package by.intexsoft.call.constant;

/**
 * Class constants for {@link by.intexsoft.call.config.RabbitMqConfig}
 */
//todo make this class abstract
public class RabbitMqConstant {
    public static final String EXCHANGE = "direct_exchange"; //todo replace in app
    public static final String CALL = "call"; // todo accumulate in one place (see Type enum)
    public static final String SMS = "sms"; // todo accumulate in one place (see Type enum)
    public static final String MMS = "mms"; // todo accumulate in one place (see Type enum)
}
